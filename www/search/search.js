'use strict';

// search.js

// const
//     config = require('../config.js'),
//     engine = require('./' + config.search.provider).createSearchEngine(config.search.configs);

// console.log('build search engine: ' + config.search_provider);
// console.log('external: ' + engine.external);

// module.exports = {
//     engine: engine
// };
